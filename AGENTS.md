## Codex
- 2026-07-17: Clarified every Glyph company-reference line so the case study now explicitly says Nothing, the company, was used only as a class reference and did not sponsor or participate in the project; also replaced the vague “brand reference” and “pick a brand” wording throughout the custom case study and project data, then verified the production build.
- 2026-07-15: Cleaned up the bespoke Glyph case study after visual review by rewriting the story in plain first-person language, removing em-dash and portfolio-jargon phrasing, replacing the unrelated display treatment with a code-built Nothing-adjacent dot-matrix GLYPH wordmark, and unifying the page into one continuous dark editorial system with light surfaces reserved for product/reference media; verified the production build plus 1280px desktop and 390px phone layouts, authentic asset loading, undistorted portrait media, zero horizontal overflow, filters, carousel, Escape/Back/close behavior, focus return, and no browser errors.
- 2026-07-15: Rebuilt Glyph as a bespoke full-screen physical-AI case study for hiring reviewers, leading with the tinker-first and embodied-AI story, adding Michael Saubert course context and official Nothing reference carousel, presenting six approved AI-assisted prototype renders at their natural portrait ratios, separating looks-like/works-like proof from future intent, optimizing all new local media to WebP, and relabeling the Selected Work card as Prototype; verified the production build, every Glyph filter entry point, carousel, close/Escape/Back/focus-return behavior, exact image ratios, truth-boundary copy, 1280×800 desktop and 390×844 mobile layouts, zero overflow, and no console, page, request, or asset errors.
- 2026-07-13: Corrected the Glyph portfolio regression by removing the hallucinated one-off case-study renderer, restoring the shared Selected Work card/detail format, reconnecting the byte-identical AI-cleaned presentation renders from the authoritative `glyph_portfolio_page/assets` set, rewriting the project data around the shipped two-model prototype without unsupported feature claims, and normalizing shared hero/carousel media to undistorted 16:10 frames; verified the production build plus 1280px desktop and 390px phone layouts with zero overflow, all approved Glyph assets loaded, and no browser errors beyond the existing Tailwind CDN warning.
- 2026-07-13: Reframed Selected Work around visitor hiring questions—works across disciplines, makes AI tangible, shapes the product, and builds the system—with overlapping project membership, a deliberately curated project order per lens, and a quiet selector derived from the existing glass/indigo navigation system; verified the production build plus desktop/mobile content, ordering, interaction, and overflow states with no console or page errors.
- 2026-07-12: Finished the Glyph evidence-first portfolio rebuild from `PORTFOLIO_PAGE_HANDOFF.md` with a dedicated black/white/signal-red editorial case study, explicit looks-like versus works-like separation, authentic-photo placeholders, the current lift/point/snap/adjust/confirm firmware loop, a built-versus-intent truth matrix, quarantined synthetic asset provenance, and a fixed in-dialog “Read the evidence” action; verified the production build, 1280px desktop and 390px mobile layouts with zero overflow or raster product imagery, Escape-close behavior, and no browser errors beyond the existing Tailwind CDN warning.
- 2026-07-12: Repositioned the portfolio around “I make AI tangible,” corrected the React + Vite, Food Rescue (~50%), and ASEAN Top 10 claims, replaced the Antigravity-only build credit with visible Antigravity/Codex/Claude marks, added visitor-intent Selected Work filters, removed the public Studio entry point and made `/studio` development-only/noindex, added canonical/share/Person structured metadata plus sitemap/robots/manifest assets and image-loading hints, and verified desktop/mobile production previews, filters, metadata, and the production Studio guard with no console or page errors; left Northwestern dates and the résumé untouched as requested.
- 2026-07-12: Reworked Glyph into an evidence-first case study with a custom detail view, inline diagram/card artwork, in-progress factual project copy, and `glyph-asset-provenance.md` documenting quarantined synthetic assets; verified with `npm run build`.
- 2026-07-12: Added SEO/share metadata, favicon/manifest/robots/sitemap files, lazy/priority image loading hints, a dev-only `/studio` route, corrected the portfolio timeline stack copy to React + Vite, and tightened the Jarvis award wording; verified with `npm run build`.
- 2026-07-12: Added public Selected Work visitor-intent filters, removed the exposed Studio edit trigger from the Projects section, and verified with `npm run build`.
- 2026-07-12: Prepared the accumulated portfolio changes for GitHub by excluding temporary screenshot artifacts, staging the cleaned portfolio/docs/assets changeset, and verifying `npm run build` before pushing.
- 2026-07-12: Restyled the homepage floating hero avatar with built-in imagegen using only the existing cartoon artwork as reference, shifting it toward a tactile industrial-design maquette aesthetic with cleaner six-discipline tool hierarchy, saved the transparent result as `public/images/adi_floating_avatar_workshop.png`, updated `USER_IMAGE_URL` while preserving the prior avatar as a rollback, and verified the production build plus desktop/mobile browser framing with no failed requests or console errors.
- 2026-07-12: Completed a full portfolio assessment across positioning, recruiter scanability, selected-work storytelling, desktop/mobile UX, accessibility, performance, SEO/shareability, engineering health, and the linked resume; verified the production build and live flows (intro, timeline, projects, Glyph detail, profile, writings), found no failed browser requests, and identified the highest-priority risks as conflicting public facts/stack claims, an outdated resume, uncurated project hierarchy, exposed Studio controls/hidden draft writings, missing deep links/metadata, ~12.4 MB eager first-load media, Tailwind CDN production use, a 743 KB JS bundle, and four TypeScript errors; no product code changed.
- 2026-07-12: Reassessed Portfolio Studio from scratch through source inspection and live desktop/mobile testing, then replaced `PORTFOLIO_STUDIO_PLAN.md` with a product-first blueprint that rejects the long property-form/mock-preview direction in favor of a dedicated `/studio` visual block composer using the exact public renderers, a versioned V2 project/asset contract, inline editing and contextual controls, local recovery, Git-backed one-click publishing, validation/conflict handling, rollback, phased implementation, and explicit definition of done; confirmed the current build still succeeds and documented the existing 3,745px mobile workflow, dormant/uneditable fields, ID-specific rich layouts, base64 drafts, and missing publish path as reasons for replacement.
- 2026-07-12: Fully re-rendered the homepage floating hero avatar with built-in imagegen using only the existing cartoon avatar as the composition reference, removed the chroma-key background into `public/images/adi_floating_avatar_hq.png`, kept `USER_IMAGE_URL` on the local asset, and verified with `npm run build` plus a production-preview Playwright check showing the 1448x1086 PNG loading cleanly with no failed requests.
- 2026-07-06: Fixed the actionable P2/P3 accessibility findings from `ACCESSIBILITY_AUDIT.md` — (state ARIA) `aria-pressed` on Studio theme-color buttons, `aria-current` on the active sidebar project, `aria-expanded` on Hero skill toggles (verified flipping on activation); (Studio) surfaced the specific `draftError` as visible `role="alert"` text (was tooltip-only), changed the stray inner `<main>` to a `<div>` (doc now has exactly one `main`), resolved the invalid nested `<label>` via a `Field asGroup` variant for the two MediaDropzone fields (`label label` count = 0), and named the JSON import input; (alt text) replaced all generic `alt="Profile"/"Logo"/"Thumbnail"/"Project"/"TV"` with descriptive strings from item data (0 generic alts remain); (misc) gave the writings-unlock button `aria-label="Read my writings"` + keyboard-focus label reveal, and set the dead `ProjectModal` embed to `autoplay=0`. Verified in the production build. Intentionally left (documented in the audit, not oversights): sub-12px text and non-nav touch targets (not 2.1 AA failures — AAA/2.2 respectively), the brand-accent "Case Study" badge (owner's design call), and O-12 inert-off-screen (defers to contended `App.tsx`). All P0/P1/P2/P3 accessibility work is now complete; spec in `ACCESSIBILITY_FIX_SPEC.md`.
- 2026-07-06: Fixed all P1 accessibility findings from `ACCESSIBILITY_AUDIT.md` — (contrast) raised dark-theme muted-text tokens `text-white/{20–45}` used for readable text/icons to `/55`–`/60` across ~17 components, darkened CaseStudyModal + TimelineEvent light-deck grays/accents to ≥4.5:1, and gave Studio inputs a visible `border-white/40` + `placeholder:text-white/50`; (landmarks/headings) added a `Skip to content` link, `role="main"`/`id="main-content"` on the scroll container, `<nav aria-label="Sections">` around VerticalNavbar, made the Hero name the single page `<h1>` (tagline→`<p>`) and demoted the 5 modal titles to `<h2>`; (motion) wrapped the app in `<MotionConfig reducedMotion="user">`, made `smoothScrollTo` + ScrollTracker scroll fall back to instant under `prefers-reduced-motion`, added an `index.html` reduced-motion `@media` block, and gave both CSS marquees `:focus-within` pause + `animation:none` under reduced motion. Verified in the production build: 0 meaningful contrast fails in intro/fit/ExperienceDetail/CaseStudyModal, skip link first in tab order → `#main-content`, single `h1`, 1 `main` + 2 `nav`, MotionConfig + reduced-motion guards present. Left the white-on-accent "Case Study" badge (3.76:1, needs per-theme shade tuning) as a noted P3 follow-up. Spec in `ACCESSIBILITY_FIX_SPEC.md`.
- 2026-07-06: Fixed all P0 accessibility findings from `ACCESSIBILITY_AUDIT.md` — made every click-only timeline/nav/profile control keyboard-operable (role/tabIndex/Enter-Space or real buttons, `aria-current` on nav, 44px mobile targets, keyboard intro-dismiss + "Explore timeline" button, `ScrollTracker` buttons), and added a shared `hooks/useDialogA11y.ts` (initial focus, Tab trap, Escape-close, focus restore, browser-Back-close) retrofitted onto all 9 overlays with `role="dialog"`/`aria-modal`/`aria-labelledby` + named icon buttons + Studio `role="status"`, switching every close path from `history.back()` to a direct `onClose()`; also fixed a history-entry leak in the hook (pushState clones state, so the original reference-equality check never matched — replaced with a `modal`-tag + consumed/selfPopping flag guard). Verified in the production build (`npm run build` + `npm run preview`): keyboard opens a card → dialog announces role/title → focus traps → Escape closes → focus returns to the card; browser Back closes without navigating away; history entry consumed and stable across repeat cycles. Spec in `ACCESSIBILITY_FIX_SPEC.md`.
- 2026-07-06: Fixed the site-wide animation jank: stopped `App.tsx` from re-rendering the whole tree on every scroll frame (rail-only, rAF-coalesced `scrollTop` updates + memoized `TimelineEvent` with stable hover/open callbacks), removed the mass `blur/grayscale` filter animation that hit every non-hovered timeline card, moved rail-card positioning from animated CSS `top` to compositor `y` transforms, replaced the `maxHeight: 2000` expand hack with measured `height: auto`, converted the mouse spotlight from full-screen gradient repaints to a transformed gradient layer, throttled Hero's per-frame `getBoundingClientRect` marquee poll to a 200ms interval gated on a new `active` prop (which also pauses the hero's infinite float/marquee loops while hidden), and dropped invisible `backdrop-blur-xl` from the four ≥90%-black modal backdrops; verified with `npm run build` plus desktop and mobile browser checks (intro→timeline transition, rail zoom, hover expansion, wheel/touch scroll-dismiss) on a local Vite server.
- 2026-07-06: Rewrote `PORTFOLIO_STUDIO_PLAN.md` from a light local-first rollout note into a robust Portfolio Studio architecture plan covering owner-only auth, normalized drafts, immutable published snapshots, media storage/variants, schema validation, migration, phased implementation, testing, and definition of done.
- 2026-07-06: Corrected the homepage floating hero avatar work by replacing the prior generated file with a 2560x1920 transparent high-res rebuild made only from the original cartoon avatar, kept `USER_IMAGE_URL` pointed at `public/images/adi_floating_avatar_hq.png`, and verified with `npm run build` plus a localhost Playwright screenshot/console check; built-in imagegen rejected same-source rerender attempts, so no personal headshot/reference was used.
- 2026-07-06: Ran a full WCAG 2.1 AA accessibility audit (4-agent static sweep + live browser verification of keyboard, focus, contrast, dialogs, touch targets, and reflow across desktop/mobile/640px viewports) and wrote the findings to `ACCESSIBILITY_AUDIT.md` — 34 issues (6 critical, led by click-only timeline cards/desktop nav, zero modal focus management, history-coupled modal dismissal, and no reduced-motion support); no product code changed.
- 2026-07-06: Removed the availability language from the header badge and hero by replacing the status label, internship tooltip, and Summer 2026 internship banner with neutral profile/product positioning; verified with `npm run build`.
- 2026-07-06: Deleted nine orphaned files left dead by the `App.refactored.tsx` removal (`hooks/useModals.ts`, `hooks/useZoom.ts`, `hooks/useScroll.ts`, `hooks/useTimeline.ts`, `hooks/useFilter.ts`, `services/timelineService.ts`, `context/TimelineContext.tsx`, `components/Header.tsx`, `components/ZoomControls.tsx`) after import-tracing confirmed nothing reachable from `index.tsx` references them, keeping `CaseStudyModal`/`ProfileModal`/`ProjectModal` which `App.tsx` still imports; verified with `npm run build` plus a browser check (clean load, no console errors) on `localhost:3000`.
- 2026-07-05: Deleted six stale refactor-analysis leftovers from the repo root (`App.refactored.tsx`, `App.test.tsx`, `ARCHITECTURE.md`, `PERFORMANCE.md`, `IMPROVEMENTS_SUMMARY.md`, `DEBUG.md`) after confirming nothing imports or runs them, and removed the CLAUDE.md gotcha that listed them; verified with `npm run build`.
- 2026-06-21: Added Portfolio Studio: a live Selected Work editor with text fields, drag-and-drop hero/gallery media, local IndexedDB drafts, import/export, new-project templating, cross-portfolio runtime project state, desktop/mobile QA, and a staged authenticated publishing plan in `PORTFOLIO_STUDIO_PLAN.md`; verified `npm run build` and the Studio workflow on `127.0.0.1:3000`.
- 2026-06-11: Rebuilt the homepage hero cutting-mat background as a generator-inspired SVG surface with crisp ruler ticks, numeric guides, angle/radius construction lines, tuned desktop/mobile hierarchy, and verified `npm run build` plus desktop/mobile screenshots on `127.0.0.1:3000`.
- 2026-06-11: Rewrote the README from a generic project template into a portfolio-forward overview centered on Adi's technical skills, selected work, stack, repo map, and minimal local workflow.
- 2026-06-06: Added and structurally centered the quirky "Double diamond enthusiast" microcopy above the homepage hero figure, keeping Framer Motion animation off the centering transform and verifying `npm run build`, desktop/mobile Playwright geometry, screenshots, and the profile CTA interaction on `127.0.0.1:3000`.
- 2026-05-08: Corrected the McDonald's case study to the actual group-ordering concept from the Slides, regenerating the hero around shared cart, invite, participant, review, ready, and split states, rewriting the project copy/skills around group-order coordination, and verifying JSON, `npm run build`, card copy, modal copy, and the Figma Slides link on `127.0.0.1:3000/#projects`.
- 2026-05-08: Regenerated the McDonald's Selected Work hero from the Figma Slides screenshot reference with built-in imagegen, matching the deck's cream/yellow/red UI presentation language and fitting it to a 1600x1000 project asset; verified `npm run build`, card image dimensions, modal image, and the Figma Slides link on `127.0.0.1:3000/#projects`.
- 2026-05-08: Replaced the handmade McDonald's ordering-flow board with a built-in imagegen edit based on the original McDonald's ordering screenshot, preserving the source UI composition while polishing it as the Selected Work hero; reverified with `npm run build` and an in-app browser modal check on `127.0.0.1:3000/#projects`.
- 2026-05-08: Replaced the McDonald's project hero with a cleaner Figma Slides-inspired ordering-flow visual, added the provided Figma Slides source link to the project detail, and verified with `npm run build` plus an in-app browser modal check on `127.0.0.1:3000`.
- 2026-05-08: Restored `familysync_jpmorgan_chase.png` to the original dark FamilySync dashboard/service-flow visual and verified the promoted Selected Work card plus modal image on `127.0.0.1:3000` after `npm run build`.
- 2026-05-07: Promoted the McDonald's interaction design and FamilySync JPMorgan Chase case studies into the Selected Work project grid, removed their duplicate Northwestern nested feature cards, and verified with `npm run build` plus localhost Playwright checks on `127.0.0.1:3000`.
- 2026-05-03: Lightened the FamilySync JPMorgan Chase asset to a whiter slide-style background while preserving the foreground principle-card composition and verifying the updated MS EDI card/modal crop.
- 2026-05-03: Reworked the FamilySync JPMorgan Chase asset to match the Figma Slides design language more closely, using the deck's blue gradient palette, sparse title-slide typography, beveled principle cards, and clean portfolio-overlay-safe spacing; rechecked in the MS EDI card/modal.
- 2026-05-03: Fixed desktop audit findings by blocking intro-header clicks, making the hidden hero inert outside intro, wiring blog detail open/close into global overlay state, containing blog modal wheel events, making writing cards keyboard accessible, labeling the TinkerVerse dialog/close button, and verifying with `npm run build` plus localhost Playwright regression screenshots in `/tmp/portfolio-desktop-fix-verify-20260503`.
- 2026-05-03: Generated and normalized a JPMorgan Chase-inspired FamilySync at-a-glance asset with imagegen, wired it into the MS EDI FamilySync card and project hero, fixed JPMorgan Chase spacing in timeline/project copy, and verified JSON, `npm run build`, and localhost Playwright screenshots.
- 2026-05-03: Ran a full desktop usability audit on `localhost:3000`, covering profile, timeline experiences, selected-work project detail, writings unlock/blog detail, TinkerVerse, keyboard focus order, console output, network failures, screenshots in `/tmp/portfolio-desktop-usability-20260503`, and `npm run build`.
- 2026-05-03: Reframed Zero as a quirky Jarvis-inspired second-brain assistant, clarified the memory model as four layers, replaced confusing home/vector labels, added hover explanations for Zero stats/skills, changed the former text/image section into the Codex live-coding workspace and council, removed the home-assistance screenshot from the Zero gallery, and verified JSON validity, `npm run build`, and localhost reload.
- 2026-05-03: Rewrote the Zero case study around a recruiter-friendly human-centered assistant experiment, emphasizing voice/chat, image generation, SQL + vector memory, custom routing, prompt-injection guardrails, home context, and bug/feature loops while removing Holomat/game references; verified JSON validity, `npm run build`, and a localhost browser refresh.
- 2026-05-03: Fixed the MS EDI project visuals by restoring stable non-marquee project cards, wiring FamilySync to `familysync_care.png`, adding a cropped McDonald's ordering screenshot asset for the McDonald's project, and verifying with `npm run build` plus localhost Playwright screenshots.
- 2026-05-03: Removed the decorative lucide icon badges from Selected Work project cards so projects no longer show random pseudo-logos; verified with `npm run build` and a localhost browser refresh.
- 2026-05-03: Restored the Northwestern logo's purple rendering by removing the old invert filters from timeline/detail/mobile logo instances and setting the local SVG fill to Northwestern purple `#4E2A84`; verified with `npm run build` and localhost browser checks.
- 2026-05-03: Fixed the broken Northwestern logo by replacing the failing Wikimedia thumbnail URL with a local `northwestern_wildcats_logo.svg` asset, then verified `npm run build` and a localhost browser network check.
- 2026-04-29: Unimplemented the workshop-portfolio cleanup pass by restoring the previous Tailwind CDN/card/nav/modal/scroll behavior while preserving the earlier analytics instrumentation and Zero screenshot assets; verified with `npm run build`.
- 2026-04-28: Replaced the generated-looking Zero portfolio imagery with real 1600x1000 screenshots captured from the local Zero runtime and Holomat lab for `zero_hero_banner.png`, `zero_holomat.png`, `zero_voice_chat.png`, `zero_text_mode.png`, `zero_project_graph.png`, and `zero_home_automation.png`; verified with `npm run build` and local portfolio screenshots on `localhost:3000`.
- 2026-04-28: Added a vendor-neutral custom analytics layer in `lib/analytics.ts`, declared optional Zaraz/PostHog/Plausible/Umami browser globals, instrumented portfolio engagement events across `App.tsx`, `components/ProjectsSection.tsx`, `components/BlogSection.tsx`, `components/ProfileModal.tsx`, and `components/MobileTimeline.tsx`, then verified with `npm run build` and started Vite with analytics debug logging on `localhost:3000`.
- 2026-04-26: Fixed the mobile no-scroll flow in `App.tsx` by bridging intro hero touch, pointer-drag, and mobile-width wheel gestures into the inner timeline scroller, then added reverse mobile wheel/pointer scrolling so the in-app browser can scroll back up after moving down; kept the mobile header/modal nav cleanup in `components/VerticalNavbar.tsx` and verified with actual in-app down/up gestures, narrow-browser wheel/drag scrollTop checks, and `npm run build`.
- 2026-04-18: Removed the cross-page cutting-mat/studio background rollout from `App.tsx`, `components/ProjectsSection.tsx`, `components/BlogSection.tsx`, and `components/TimelineRail.tsx`, restoring the cleaner non-hero sections while keeping the homepage mat; re-verified with `npm run build`.
- 2026-04-18: Extended the homepage cutting-mat/studio theme across the site in `App.tsx`, `components/ProjectsSection.tsx`, `components/BlogSection.tsx`, and `components/TimelineRail.tsx`, including global workshop grid overlays, themed Projects/Writings panels, and cleaned-up margin labels; re-verified on `localhost:3000`.
- 2026-04-18: Restored the homepage compact GitHub floater’s frosted-glass feel in `components/GitHubActivity.tsx` while removing the thick white border treatment, using blur, translucent fills, subtle inset highlights, and shadow for depth; re-verified on `localhost:3000`.
- 2026-04-18: Removed the thick white border treatment from the homepage compact GitHub floater in `components/GitHubActivity.tsx` and kept depth through darker shadows plus subtle inset highlights; re-verified on `localhost:3000`.
- 2026-04-18: Restored the homepage compact GitHub floater to the stronger framed look in `components/GitHubActivity.tsx` and added darker card shadows for depth instead of further border softening; re-verified on `localhost:3000`.
- 2026-04-18: Softened the homepage compact GitHub floater border in `components/GitHubActivity.tsx` so the left-side panel blends more while staying opaque and legible; re-verified on `localhost:3000`.
- 2026-04-18: Increased the homepage compact GitHub floater opacity in `components/GitHubActivity.tsx` with denser card fills, stronger label contrast, and a more visible contribution grid; re-verified on `localhost:3000`.
- 2026-04-18: Softened the homepage hero mat perimeter and inset frame lines in `components/Hero.tsx` so the edge reads less like a bright outline and more like a tonal surface; re-verified on `localhost:3000`.
- 2026-04-18: Increased the homepage hero avatar idle drift and hover lift in `components/Hero.tsx` so the central image feels more clickable, with slightly more responsive shadow compression; re-verified on `localhost:3000`.
- 2026-04-18: Removed the bold white center guide outlines around the homepage hero avatar in `components/Hero.tsx` for a cleaner mat composition; re-verified on `localhost:3000`.
- 2026-04-18: Tuned the homepage hero avatar in `components/Hero.tsx` with a tighter floating hover loop and a darker grounded shadow beneath the image; re-verified on `localhost:3000`.
- 2026-04-18: Reworked the homepage hero mat in `components/Hero.tsx` to feel more realistic and structured with printed ruler bands, registration marks, subtler cut guides, and cleaner mobile-safe labeling; re-verified on `localhost:3000`.
- 2026-04-18: Enlarged the homepage hero mat, increased the center avatar presence/opacity, and strengthened top-name separation in `components/Hero.tsx`; re-verified on `localhost:3000`.
- 2026-04-18: Improved homepage hero text contrast in `components/Hero.tsx` and `App.tsx` by adding darker calm zones, stronger type/shadow treatment, and a more legible scroll prompt; re-verified on `localhost:3000`.
- 2026-04-18: Refined the hero surface from a cutting-board look into a real self-healing cutting mat in `components/Hero.tsx` by removing the hanging-hole cue and adding grid/ruler/guide lines; verified on `localhost:3000`.
- 2026-04-18: Added a quirky green cutting-board hero background to the landing homepage in `components/Hero.tsx`, verified desktop/mobile screenshots in `/tmp/portfolio-ui-pass-20260418`, and brought the dev server up on `localhost:3000`.
- 2026-04-18: Fixed the mobile intro overlay so touch scrolling dismisses it without resetting scroll, added keyboard-accessible project cards with focus states, and re-captured verification screenshots in `/tmp/portfolio-ui-pass-20260418`.
- 2026-04-18: Ran a desktop and mobile UI pass, captured screenshots in `/tmp/portfolio-ui-pass-20260418`, and documented the mobile intro overlay/scroll regression plus related interaction findings.
